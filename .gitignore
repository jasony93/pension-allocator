# git worktrees for isolated feature work
.worktrees/

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# node
node_modules/

# 검증기 테스트가 만드는 임시 픽스처 디렉터리 (정상 종료 시 스스로 지운다)
tests/org/fixtures/tmp-*/

# scripts/build.mjs 산출물. 이 저장소는 D66에 따라 공개하지 않는다 — 배포용
# 단일 HTML은 별도 공개 저장소로 옮겨 올린다(README "배포" 절 참고). 소스에서
# 매번 다시 만들 수 있는 산출물을 여기에 커밋하면 두 곳이 갈라질 뿐이다.
dist/
