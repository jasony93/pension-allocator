---
unit: tax-domain
stage: 4
status: approved
inputs:
  - docs/stage-4-verification/golden-cases.md
  - data/tax-rules/2026.json
  - data/tax-rules/2027-proposed.json
  - docs/stage-2-design/engine-interface.md
  - docs/org/gate-decisions.md
open_questions:
  - "**[3차 재검증]** M1·M2·M3이 전부 해소됐고 골든 케이스 35건 전건 일치다(9절). 남은 미확인 항목은 두 가지뿐이며 둘 다 이 유닛의 범위 밖이다 — 계약 9절 드리프트 방지 규약(`qa`)과 O1의 화면 처리(`designer`·`web-dev`)."
  - "**[3차 재검증]** 불변식 17개는 테스트 파일을 열지 않았으므로 **검증하지 않았다.** 이 유닛이 확인한 것은 골든 케이스 35건의 입출력뿐이다. 불변식과 골든 케이스는 서로를 대체하지 않는다 — M1은 자기모순이 아니라 '세법상 최적이 아닌' 것이어서 불변식으로는 잡히지 않았을 사안이다(9.7절). 두 장치를 모두 유지할지는 관리자 판단이다."
  - "**[3차 재검증]** GC-21은 1차·2차에서 각각 다른 원인으로 걸렸다가 3차에서 깨끗해졌다. 두 번 모두 '어떤 사실이 조건부로 바뀌었는데 그 사실에 의존하던 다른 출력이 따라오지 못한' 형태였고, 원인은 **입력 자체가 모순인 케이스**라는 데 있다(의무가입기간이 끝난 사람이 그 기간 안에 쓰겠다고 답한 상태). 경고·안내 조건을 건드리는 수정에서는 GC-21과 GC-29를 함께 돌리기를 권한다(9.6절)."
  - "**[2차 재검증]** M1·M2는 커밋 `1a33f7e`로 해소됐다(8.2·8.5절). 확정 시나리오 25건의 값은 하나도 움직이지 않았다(8.3절). 대신 **M3이 새로 드러났다** — 비교 안내 `all_accounts_have_early_exit_penalty`가 M2 수정 이후에도 horizon만 보고 나가서, ISA 경고가 사라진 케이스에서 '어느 배분안도 불이익을 피하지 못한다'는 사실과 다른 안내를 낸다. 계약 8.5절을 8.4절과 맞추는 것이 맞다고 보나 게이트 2 고정분이라 관리자 승인이 필요하다(8.6절)."
  - "**[2차 재검증]** M3은 M1과 같은 형태의 실패다 — 어떤 전제가 조건부로 바뀌었는데 그 전제를 공유하던 다른 출력이 따라오지 못했다. M1은 D17의 파급을 배분 탐색이, M3은 M2의 파급을 비교 안내가 놓쳤다. **출력 필드 사이의 사실 일관성을 기계적으로 검사할 장치가 없다는 것이 공통 원인으로 보인다** — qa에게 검사 항목으로 넘겼으나 구조적 해법이 필요한지는 관리자 판단이다."
  - "M1(개정안 시나리오에서 최대공제안이 세액공제를 최대화하지 못함)은 엔진을 고칠 사안으로 보이지만, 고치는 방향이 배분 탐색의 목적함수를 바꾸는 것이라 계약의 plan_id 의미(`max_tax_credit`)와 화면 비교 로직에 파급이 있다. 엔진 수정인지 계약 수정인지는 관리자 판정이다."
  - "M2(ISA 의무가입기간 경과 후에도 중도해지 추징 경고가 나감)는 엔진이 engine-interface.md 8.4절을 그대로 구현한 결과다. 즉 결함의 소재가 코드가 아니라 계약일 수 있다. 계약은 게이트 2에서 고정됐으므로 변경에 관리자 승인이 필요하다."
  - "M1은 D17(합산한도 절단 시 IRP 우선 인정)을 전제로 성립한다. 반대 해석이면 애초에 추가 IRP 납입이 연금저축분을 밀어내지 못하므로 M1 자체가 성립하지 않는다. 즉 M1의 존재가 D17이라는 해석 선택에 의존한다 — 법안이 국회를 통과하면 두 가지를 함께 재검토해야 한다."
  - "이번 검증은 compute() 진입점만 대조했다. computeFundUseHorizonBoundaries()와 compute()의 boundaries가 바이트 단위로 같아야 한다는 계약 9절의 드리프트 방지 규약은 대조하지 않았다. 단위 테스트로 고정하기로 되어 있으므로 qa가 게이트 4에서 확인할 항목이다."
  - "룰셋에 있으나 이번 케이스가 건드리지 않은 규칙이 넷 있다 — pension.income.withholding_rate.by_age, pension.income.withholding_rate.lifetime_annuity, pension.income.separate_taxation.threshold, pension.income.separate_taxation.elective_rate. 전부 인출 단계 규칙이고 게이트 1 D3이 인출 단계를 v2로 미뤘다. v2 착수 시 골든 케이스를 추가해야 한다."
---

# 4단계 검증 — 엔진 결과 대조 리포트

- 1차 검증일: 2026-08-09 (계약 `schema_version` 3.0.0, 골든 케이스 29건)
- **2차 재검증일: 2026-08-09** (계약 `schema_version` 3.1.0, 골든 케이스 **34건**) — `calc-engine-dev`의 M1·M2 수정(커밋 `1a33f7e`) 대상
- 검증 유닛: `tax-domain`
- 대상: `compute()`
- 골든 케이스: `docs/stage-4-verification/golden-cases.md`

**이 문서는 1차 기록을 지우지 않는다.** 아래 0~7절이 1차 검증 그대로이고, **8절이 2차 재검증**이다. M1·M2가 있었다는 사실과 그 원인 분석이 남아야 같은 자리가 다시 깨졌을 때 첫 단서가 된다.

## 0. 결과 요약 (1차)

| 구분 | 건수 |
|---|---|
| 대조한 케이스 | **29** |
| 일치 | **25** |
| 불일치 | **4** (근본 원인 **2**개) |
| 3회 반복 불일치로 에스컬레이션할 건 | **0** |

불일치 4건 중 3건(GC-19 개정안분 · GC-19c · GC-23)은 **하나의 결함(M1)**이고, 1건(GC-21)은 별개의 결함(M2)이다.

**독립성 준수.** 이번 작업에서 `src/engine/`과 `src/web/`은 한 줄도 읽지 않았다. 엔진은 실행만 했다. 아래 원인 분석은 전부 **입출력 관찰에서 역으로 좁힌 것**이며, 코드를 근거로 삼은 문장은 하나도 없다.

## 1. 일치한 25건

| 케이스 | 확인한 핵심 값 | 결과 |
|---|---|---|
| GC-01 | 15% 구간, 1,350,000 / 135,000 / 1,485,000, ISA 잔여 20,000,000, 비과세 2,000,000, 경계 3년·15년 | 일치 |
| GC-02 | 12% 구간, 1,080,000 / 108,000 / 1,188,000 | 일치 |
| GC-03 | 15% 구간, 1,350,000 / 135,000 / 1,485,000 | 일치 |
| GC-04 | 연금 9,000,000(credit_limit) + ISA 20,000,000(contribution_limit), 미배분 1,000,000, **잔차 8** | 일치 |
| GC-05 | 해당연도 12% · 직전연도 기준 서민형 4,000,000, 충돌 경고 없음, 792,000 | 일치 |
| GC-06 | 선언대로 4,000,000 유지 + `isa_type_conflicts_with_prior_income`(warning) | 일치 |
| GC-07 | 공제 잔여 0·클램프, 납입 잔여 6,000,000, 인정액 9,000,000, 1,485,000, `existing_contribution_over_limit` | 일치 |
| GC-08 | 예산 0, 공제 0, `zero_capacity` + `plans_collapsed_single`, 한도는 정상 산출 | 일치 |
| GC-09 | 배분 29,000,000 · 미배분 211,000,000, `budget_exceeds_all_limits` | 일치 |
| GC-10 | ISA 잔여 **70,000,000**(경과 5년이 4년으로 상한), 의무기간 잔여 0 | 일치 |
| GC-11 | ISA 잔여 **0**, 미배분 15,000,000 | 일치 |
| GC-12 | ISA 잔여 **60,000,000**(총한도에서 재형저축 40,000,000 차감) | 일치 |
| GC-13 | ISA `eligible: false` / `isa_excluded_financial_income_taxpayer`, 미배분 21,000,000 | 일치 |
| GC-14 | ISA `isa_excluded_age`, 비과세 한도 null, 990,000, 연금 잔여 **41년**, `pension_age_not_evaluated` | 일치 |
| GC-15 | 추가한도 **3,000,000**(300만원 상한 구속), 합산한도 12,000,000, **1,980,000** | 일치 |
| GC-16 | 추가한도 **1,000,000**(10% 구속), 합산한도 10,000,000, **1,320,000** | 일치 |
| GC-17 | 추가한도 **1,800,000**(직전 적용액 1,200,000 차감), **1,782,000** | 일치 |
| GC-18a | 금액 불변, 세 계좌 경고 `warning`, `all_accounts_have_early_exit_penalty` | 일치 |
| GC-18b | 금액 불변, 기본안 `isa_first`, **delta 양수 1,485,000**, `baseline_reordered_by_fund_use_horizon` | 일치 |
| GC-18c | 금액 불변, 경고 없음 | 일치 |
| GC-18d | 금액 불변, `fund_use_horizon_not_declared` | 일치 |
| GC-19 (확정분) | 1,080,000 / 108,000 / 1,188,000 | 일치 |
| GC-19b | 확정 1,188,000 · **개정안 1,386,000** (D17 인정 로직) | 일치 |
| GC-20 | 두 시나리오 모두 1,188,000, `youth_status_not_declared`, 우대 규칙이 `unapplied`에 실림 | 일치 |
| GC-22 | 연금 월 1,285,714 · ISA 월 142,856, **잔차 7**(대안안은 14) | 일치 |
| GC-23-oracle | **1,350,000 / 135,000 / 1,485,000** | 일치 |

특기할 것 셋.

**(1) 지방소득세를 상수로 박지 않았다.** 모든 케이스에서 지방세분이 정확히 소득세분의 10%로 나왔고 `credit_rate_bracket.basis_rule_ids`에 `tax.local.personal_income_surtax`가 실렸다. 헌장 제품원칙 1과 룰셋의 `engine_note`가 요구한 것이다.

**(2) 공제율 경계가 정확히 닫혀 있다.** GC-01/02/03이 55,000,000원을 사이에 두고 1원 단위로 갈렸다. 총 297,000원이 1원 차이로 움직이는 절벽이므로 여기서 부등호가 틀리면 곧바로 사용자 피해다.

**(3) 계약 4.2절의 자기 선언이 실제로 지켜졌다.** GC-18a~d에서 배분 금액·세액공제액·한도가 네 값에 걸쳐 완전히 동일했고, 달라진 것은 순서·기본안·경고·비교 안내뿐이었다.

## 2. 불일치 M1 — 개정안 시나리오에서 최대공제안이 세액공제를 최대화하지 못한다

**해당 케이스:** GC-19(개정안분), GC-19c, GC-23 — 3건, 원인 하나.
**심각도: 높음.** 사용자에게 제시되는 권고 배분이 최적이 아니고, 그 손해가 금액으로 표시된다.

### 2.1 관측된 값

| 케이스 | 골든 케이스 (소득세/지방세/합계) | 엔진 결과 | 차이(합계) |
|---|---|---|---|
| GC-19 개정안분 | 1,260,000 / 126,000 / **1,386,000** | 1,170,000 / 117,000 / **1,287,000** | **−99,000** |
| GC-19c | 1,260,000 / 126,000 / **1,386,000** | 1,170,000 / 117,000 / **1,287,000** | **−99,000** |
| GC-23 | 1,350,000 / 135,000 / **1,485,000** | 1,170,000 / 117,000 / **1,287,000** | **−198,000** |

GC-23에서 엔진의 최대공제안 배분은 `retirement_pension: 3,000,000 (limited_by: credit_limit)` + `isa: 9,000,000`이었다. 골든 케이스의 답은 `retirement_pension: 9,000,000` + `isa: 3,000,000`이다.

### 2.2 세법 근거와 계산 과정

프로필(GC-23): 청년 자기신고 있음, 총급여 60,000,000원(공제율 경계 위), **연금저축 기납입 6,000,000원**, IRP 기납입 0, 예산 12,000,000원.

**엔진의 답 — IRP 3,000,000**
- 인정: `IRP 3,000,000` + `연금저축 3,000,000`... 이 아니라, D17에 따라 IRP 3,000,000을 먼저 인정하고 연금저축은 min(6,000,000, 9,000,000−3,000,000) = 6,000,000 → 합 9,000,000.
- 공제: 3,000,000 × 0.15 + 6,000,000 × 0.12 = 450,000 + 720,000 = **1,170,000** 소득세.

**골든 케이스의 답 — IRP 9,000,000**
- `pension.contribution.annual_limit` 잔여 = 18,000,000 − 6,000,000 = 12,000,000 ≥ 9,000,000 → 납입 가능.
- 인정(소득세법 §59조의3① 단서 + D17): 연금저축 단독 한도 6,000,000 적용 후, 합산 9,000,000을 **IRP부터** 채운다. `IRP인정 = min(9,000,000, 9,000,000) = 9,000,000`, `연금저축인정 = min(6,000,000, 6,000,000, 9,000,000−9,000,000) = 0`.
- 공제: 9,000,000 × 0.15(`proposed.pension.credit.youth_irp_rate`) + 0 = **1,350,000** 소득세.

**차이의 정체.** 추가 IRP 납입은 잔여 공제한도를 채우는 것에 그치지 않는다. D17의 IRP 우선 인정 아래에서는 **이미 납입된 연금저축분을 공제 대상 풀에서 밀어낸다.** 밀려난 3,000,000~6,000,000원은 12%에서 15%로 갈아탄 것과 같다. 잔여 공제한도(3,000,000)를 상한으로 보면 이 치환을 놓친다.

### 2.3 엔진의 인정 로직은 옳다 — 배분 탐색만 틀렸다

이 구분이 수정 범위를 정하므로 별도 케이스로 격리해 확인했다.

- **GC-19b**(기납입 IRP 6,000,000 + 연금저축 6,000,000, 예산 0): 엔진이 개정안 시나리오에서 **1,386,000**을 냈다. 골든 케이스와 일치한다.
- **GC-23-oracle**(기납입 IRP 9,000,000 + 연금저축 6,000,000, 예산 0): 엔진이 **1,485,000**을 냈다. 골든 케이스와 일치한다.

즉 **엔진은 골든 케이스가 지목한 최종 상태를 주면 골든 케이스와 같은 공제액을 계산한다.** D17 인정 로직도, 청년 15% 적용도, 지방세 부가도 전부 맞다. 틀린 것은 **그 상태에 도달하는 배분을 찾지 못한다**는 점 하나다.

관측에서 좁힌 원인: 최대공제안이 IRP 배분을 `limits.pension_combined_credit_remaining_krw`(=3,000,000)에서 멈춘다. 출력의 `limited_by: "credit_limit"`가 그 상한을 그대로 가리킨다. 잔여 공제한도는 **기납입 배분이 고정이라는 가정 아래에서만** 상한이고, D17이 그 가정을 깬다.

### 2.4 왜 확정 시나리오에서는 안 나타나는가

두 연금계좌의 공제율이 같기 때문이다. GC-19 확정분에서 IRP를 3,000,000으로 하든 9,000,000으로 하든 인정 합계는 9,000,000이고 전부 12%이므로 공제액이 같다(1,188,000). GC-07도 마찬가지다.

**이것은 `tax-rules-report.md` 6.5절이 예고한 그대로다.** "공제율이 계좌 종류와 무관했으므로 초과분이 어느 계좌에서 나오든 공제액 합계가 같았다. 개정안이 IRP에만 다른 율을 주면 처음으로 귀속이 세액을 바꾼다." 귀속이 세액을 바꾸는 순간, 배분 탐색도 함께 바뀌어야 했는데 그 파급이 반영되지 않았다.

### 2.5 노출 범위

노출은 **개정안 시나리오에 한정**되고, 그 안에서도 다음 조건이 모두 성립할 때만 발생한다.

1. `declared_youth: true`
2. 소득이 15% 경계 **위**(경계 아래면 두 계좌 모두 15%라 차이가 없다)
3. **연금저축 기납입액이 이미 있다**(0이면 IRP가 합산한도를 먼저 다 가져가므로 문제가 없다)
4. 예산이 잔여 공제한도보다 크다

최대 격차는 연금저축 기납입이 단독 한도 6,000,000을 채웠을 때의 **198,000원**(GC-23)이다.

6.5.6절에서 예고한 대로 **권고 배분을 처음부터 만드는 사용자에게는 발생하지 않고, 기존 납입액을 입력한 사용자에게만 발생한다.**

### 2.6 판정 요청

**엔진 수정 사안으로 본다.** 룰셋은 고칠 것이 없다 — `pension.credit.limit.combined`의 `allocation_of_excess`와 `proposed.pension.credit.youth_irp_rate`의 `allocation_dependency`가 이미 이 상호작용을 명시하고 있고, 엔진의 인정 로직은 그대로 옳다.

다만 수정 방향이 **배분 탐색의 목적함수**를 건드리므로 계약의 `plan_id: "max_tax_credit"`가 뜻하는 바(진짜 최대인가, 잔여 한도까지인가)와 `delta_vs_baseline_krw` 비교가 함께 움직인다. 관리자 판정이 필요하다.

## 3. 불일치 M2 — 의무가입기간이 경과한 ISA에도 중도해지 추징 경고가 나간다

**해당 케이스:** GC-21 — 1건.
**심각도: 중간.** 금액은 틀리지 않는다. 성립할 수 없는 법적 불이익을 사용자에게 고지한다.

### 3.1 관측된 값

GC-21(56세, ISA 가입경과 3년, horizon `within_isa_lock_in`)에서 엔진은

- `fund_use_horizon_boundaries.isa_lock_in_years_remaining: 0` — 의무가입기간이 이미 끝났음을 스스로 산출했고,
- 그럼에도 ISA 배분에 `early_termination_clawback_isa` 경고를 `severity: "warning"`으로 냈다.

### 3.2 세법 근거

`isa.early_termination.clawback`(조세특례제한법 §91조의18⑦)의 구성요건은 "계약기간이 **3년이 되는 날 전에** 계약을 해지하는 경우"다. 가입 후 3년이 경과했으면 이 요건은 성립할 수 없고, 추징을 근거로 한 경고도 성립하지 않는다. 룰셋의 규칙 `conditions`도 `isa.terminated_before_3y: true`를 요건으로 적고 있다.

같은 케이스의 연금 쪽 경고는 **오답이 아니다.** 56세로 `pension.withdrawal.eligibility`의 연령요건(55세)은 넘겼지만 5년 보유요건은 입력이 없어 판정되지 않았고(`pension_holding_period_evaluated: false`), 요건 미충족 가능성이 남아 있다. 엔진이 이 사실을 `pension_holding_period_not_evaluated` 안내로 드러내고 있어 처리가 정직하다.

### 3.3 결함의 소재가 코드가 아닐 수 있다

`engine-interface.md` 8.4절은 이 경고의 조건을 **"배분액 > 0 이고 `fund_use_horizon`이 `within_isa_lock_in`"**으로만 정한다. `isa_lock_in_years_remaining`을 조건에 넣지 않았다. **엔진은 계약을 정확히 구현했다.**

따라서 고칠 곳은 셋 중 하나이고, 이 유닛이 정할 수 없다.

1. 계약 8.4절의 조건에 `isa_lock_in_years_remaining > 0`을 추가한다(계약 변경 — 게이트 2 고정분이므로 관리자 승인 필요).
2. 엔진이 계약을 넘어 잔여 0인 경우를 걸러낸다(계약과 구현이 어긋나므로 권하지 않는다).
3. 그대로 두고 화면에서 처리한다(세법상 성립하지 않는 고지를 내보내는 것이므로 권하지 않는다).

부수 관찰 하나 — 사용자가 `within_isa_lock_in`을 골랐는데 잔여가 0이라는 것은 **입력 자체가 이미 모순**이다. 선택지 캡션이 실제 연수를 보여주게 되어 있으므로(계약 9절) 화면에서 이 조합이 잘 나오지 않을 수 있으나, 엔진 계약은 그것을 전제하지 않는다.

## 4. 불일치가 아닌 관찰 1건

**O1 — ISA가 배제된 사용자에게도 비과세 한도가 표시된다.** GC-13에서 ISA `eligible: false`(금융소득종합과세 대상자)인데 `tax_free_limit_krw: 4,000,000`이 함께 나왔다. 계약 5.3절은 이 필드를 `account_type`이 null일 때만 null로 정하므로 **엔진은 계약대로**다. 금액 계산에도 영향이 없다(ISA 배분이 0이다).

다만 과세특례가 배제된 계좌에 비과세 한도를 표시하면 사용자가 혜택이 남아 있다고 읽을 수 있다. **화면 처리 사안으로 `designer`·`web-dev`에게 넘긴다.** 불일치로 세지 않았다.

## 5. 반복 불일치 · 에스컬레이션

**에스컬레이션할 건 없다.** M1·M2 모두 이번이 **1회차** 불일치다. 헌장 6.4절의 "같은 케이스 3회 반복 불일치" 요건에 해당하지 않는다.

다만 M1에는 조건이 하나 붙는다. M1은 **D17을 전제로만 성립한다.** 반대 해석(연금저축 우선 인정)에서는 추가 IRP 납입이 연금저축분을 밀어내지 못하므로 엔진의 현재 동작이 오히려 정답이 된다. **즉 M1을 고치는 것은 D17을 코드에 한 겹 더 새기는 일이다.** 청년 IRP 우대가 국회를 통과하는 시점에 D17과 M1 수정을 함께 재검토해야 한다(게이트 3 D17이 이미 그 재검토 시점을 지정해 두었다).

## 6. 재현 방법

엔진을 `src/`를 읽지 않고 실행한 절차다. 관리자·`qa`·`calc-engine-dev`가 같은 값을 재현할 수 있다.

1. `data/tax-rules/2026.json`과 `2027-proposed.json`을 파싱해 파일명을 키로 하는 `RulesetBundle`을 만든다.
2. `src/engine/index.mjs`의 `compute(request, rulesets)`를 호출한다.
3. 요청은 `golden-cases.md`의 각 케이스 프로필을 계약 3절 형식으로 옮긴 것이다. 공통값: `schema_version: "3.0.0"`, `tax_year: 2026`, `options: null`.

**계약 문서의 표기 불일치 하나.** `engine-interface.md` 0절은 현재 계약 버전을 `3.0.0`으로 정하는데, 3.0절 `schema_version` 필드 설명은 아직 `"2.1.0"`으로 적혀 있다. 엔진은 `3.0.0`을 받는다. 두 버전은 major가 달라 한쪽은 `schema_version_mismatch`가 되므로 **문서를 읽고 요청을 만드는 사람이 실제로 걸린다.** 세법 사안이 아니므로 불일치로 세지 않았고, `calc-engine-dev`가 정정할 항목으로 넘긴다.

## 7. 다음 단계로 넘기는 사항

- **관리자** — 판정 둘. (a) M1을 엔진 수정으로 처리할지, 그 경우 계약의 `max_tax_credit` 의미를 함께 정리할지(2.6절). (b) M2를 계약 수정으로 처리할지(3.3절).
- **`calc-engine-dev`** — M1은 인정 로직이 아니라 배분 탐색 문제다(2.3절). 격리 케이스 GC-19b·GC-23-oracle이 인정 로직의 정확성을 이미 증명했으므로 그쪽은 건드릴 필요가 없다. 계약 3.0절의 `schema_version` 표기도 함께 정정한다(6절).
- **`qa`** — (a) 계약 9절의 드리프트 방지 규약(두 진입점의 `boundaries` 동일성)이 단위 테스트로 고정되어 있는지 확인한다. 이번 검증에서 다루지 않았다. (b) O1의 화면 처리를 검사 항목에 넣는다.
- **`designer`·`web-dev`** — O1(배제된 ISA의 비과세 한도 표시)과 M2의 경고 문구가 화면에서 어떻게 보이는지 점검이 필요하다.
- **4단계 이후의 나 자신** — 청년 IRP 우대가 국회를 통과하면 (a) D17을 재검토하고 (b) GC-19·19b·19c·20·23·23-oracle의 기대값을 재산출하며 (c) M1 수정 결과를 다시 대조한다. 인출 단계를 v2로 열 때는 미사용 규칙 4건에 대한 골든 케이스를 추가한다(머리말 `open_questions`).

---

# 8. 2차 재검증 — M1·M2 수정 대조 (2026-08-09)

대상 커밋 `1a33f7e`, 계약 `schema_version` 3.0.0 → **3.1.0**(minor).

**독립성은 이번에도 유지했다.** `src/engine/`·`src/web/`을 한 줄도 읽지 않았다. 수정 커밋의 **파일 목록만** 확인했고 내용은 열지 않았다. 계약 문서는 8.2·8.4·8.5절과 버전 이력만 읽었다(입출력 형식 확인 목적). `engine-design.md`는 이번 커밋에서 함께 바뀌었으나 읽지 않았다. **엔진 유닛이 추가한 회귀 테스트(`golden-regression.test.mjs`)도 열지 않았다.**

## 8.1 결과 요약 (2차)

| 구분 | 건수 |
|---|---|
| 대조한 케이스 | **34** (기존 29 + 신규 5) |
| 일치 | **33** |
| 불일치 | **1** (신규 발견 **M3**) |
| 해소 확인 | **M1 · M2 둘 다 해소** |
| 3회 반복 불일치로 에스컬레이션할 건 | **0** |

## 8.2 M1 해소 — 확인됨

| 케이스 | 골든 케이스 | 1차 (수정 전) | 2차 (수정 후) | 판정 |
|---|---|---|---|---|
| GC-19 개정안분 | 1,260,000 / 126,000 / **1,386,000** | 1,287,000 (−99,000) | **1,386,000** | 해소 |
| GC-19c | 1,260,000 / 126,000 / **1,386,000** | 1,287,000 (−99,000) | **1,386,000** | 해소 |
| GC-23 | 1,350,000 / 135,000 / **1,485,000** | 1,287,000 (−198,000) | **1,485,000** | 해소 |

배분도 골든 케이스대로 바뀌었다. GC-23의 최대공제안은 `retirement_pension: 3,000,000 + isa: 9,000,000`에서 **`retirement_pension: 9,000,000 + isa: 3,000,000`**으로, GC-19c는 IRP 3,000,000에서 **6,000,000**으로 옮겨 갔다.

**격리 케이스도 그대로다.** GC-19b(1,386,000)와 GC-23-oracle(1,485,000)은 1차와 값이 같다. 인정 로직은 원래 옳았고 이번 수정이 그것을 건드리지 않았다는 뜻이다.

## 8.3 확정 시나리오 25건 — 값이 하나도 움직이지 않았다

엔진 유닛이 **"두 공제율이 같으면 기존 상한을 그대로 쓴다"**는 조건을 달았다고 보고했다. 그 주장이 사실인지 직접 대조했다.

**1차 실행 결과와 2차 실행 결과를 케이스별로 맞춰 본 결과, 확정 시나리오 25건의 값이 전부 동일하다.** 대조한 필드는 `pension_combined_credit_limit_krw` · `pension_combined_credit_remaining_krw` · `pension_contribution_limit_remaining_krw` · 계좌별 `contribution_limit_remaining_krw` · `tax_free_limit_krw` · 최대공제안의 `allocations`(연·월 금액과 `limited_by`) · `deterministic_benefit` 4개 값 · `delta_vs_baseline_krw` · `unallocated_annual_krw` · `monthly_rounding_residual_krw`다.

특히 **배분까지 그대로**라는 점이 중요하다. GC-19 확정분과 GC-20 개정안분은 M1 수정이 걸릴 법한 형태(연금저축 기납입 6,000,000 + 잔여 공제한도 3,000,000)인데도 IRP 배분이 **3,000,000에 그대로 머물렀다.**

| 케이스 | 조건 | 2차 IRP 배분 | 판정 |
|---|---|---|---|
| GC-19 확정분 | 두 율 모두 12% | 3,000,000 (불변) | 치환 안 함 — 옳다 |
| GC-20 개정안분 | 청년 미신고 → 우대 미적용, 두 율 모두 12% | 3,000,000 (불변) | 치환 안 함 — 옳다 |
| GC-07 | 두 율 모두 15%, 한도 이미 소진 | 0 (불변) | 치환 안 함 — 옳다 |

**즉 조건은 실재하고 방향도 맞다.** 두 율이 같을 때 치환하면 공제는 한 푼도 안 늘면서 자금만 연금계좌로 옮겨 가 `pension.withdrawal.eligibility`(55세 + 5년)와 `pension.early_withdrawal.other_income_rate`(연금외수령 기타소득 15%)의 제약을 새로 지게 된다. 그 해악이 생기지 않았다.

## 8.4 신규 케이스 5건 — 조건의 양쪽을 덮었다

조건부 동작이 생겼으므로 조건의 **내부·경계·경계+1**과 **반대편**을 새로 덮었다. 전건 일치다.

| 케이스 | 무엇을 보는가 | 기대 | 결과 |
|---|---|---|---|
| GC-24 | 총급여 50,000,000 — 두 율 모두 15% | 치환 **없음**, 개정안 배분 = 확정 배분 (IRP 3,000,000 + ISA 9,000,000), 1,485,000 | 일치 |
| GC-25 | 총급여 **정확히 55,000,000** — 두 율이 같아지는 마지막 지점 | 치환 **없음**, IRP 3,000,000 + ISA 9,000,000, 1,485,000 | 일치 |
| GC-26 | 총급여 **55,000,001** — 두 율이 갈리는 첫 지점 | 치환 **있음**, IRP 9,000,000 + ISA 3,000,000, 1,485,000 (확정분은 1,188,000) | 일치 |
| GC-27 | ISA 의무가입기간 **잔여 1년** | 추징 경고가 **그대로 나감**(`warning`) | 일치 |
| GC-28 | `unknown` horizon × 잔여 **0년** | ISA 경고가 `info`로도 **안 나감**, 연금 경고는 `info`로 나감 | 일치 |

**GC-25와 GC-26이 이번 수정의 핵심 안전장치다.** 두 케이스의 개정안 세액공제액은 **둘 다 1,485,000으로 우연히 같지만 배분이 정반대**다(IRP 3,000,000 대 9,000,000). 금액만 비교하는 회귀 테스트는 조건이 뒤집혀 있어도 통과한다. 배분을 함께 고정해 두었다.

## 8.5 M2 해소 — 확인됨, 그리고 반대편도 살아 있다

| 케이스 | 잔여 의무가입기간 | ISA 추징 경고 | `isa_lock_in_already_elapsed` | 판정 |
|---|---|---|---|---|
| GC-21 (`within_isa_lock_in`) | **0년** | **사라짐** | **나옴**(info) | 해소 |
| GC-27 (`within_isa_lock_in`) | 1년 | **그대로 나옴**(warning) | 안 나옴 | 유지 확인 |
| GC-18a (`within_isa_lock_in`) | 3년 | **그대로 나옴**(warning) | 안 나옴 | 유지 확인 |
| GC-18d (`unknown`) | 3년 | **그대로 나옴**(info) | 안 나옴 | 유지 확인 |
| GC-28 (`unknown`) | 0년 | 사라짐 | 안 나옴 (계약 8.2절이 `within_isa_lock_in`으로 한정) | 계약대로 |

**조건을 좁히면서 필요한 경고까지 없애지는 않았다.** 잔여 기간이 1년이든 3년이든, horizon이 `within_isa_lock_in`이든 `unknown`이든 경고가 살아 있다. GC-21의 연금 쪽 경고도 그대로 유지됐다 — 56세로 연령요건은 넘겼지만 5년 보유요건이 판정되지 않았으므로(`pension_holding_period_evaluated: false`) 경고가 남는 것이 옳다.

## 8.6 불일치 M3 (신규) — 비교 안내가 경고와 모순된다

**해당 케이스:** GC-21 — 1건. **심각도: 중간.** 금액은 틀리지 않는다. 화면 최상단에 놓이도록 설계된 안내가 사실과 다르다.

### 관측된 값

GC-21(잔여 의무가입기간 0년, horizon `within_isa_lock_in`)에서

- ISA 추징 경고는 M2 수정대로 사라졌고,
- `isa_first` 배분안의 `warnings`가 **빈 배열**이 됐는데,
- `comparison_note_codes`에는 **`all_accounts_have_early_exit_penalty`가 그대로 실렸다.**

### 왜 틀렸는가

계약 8.5절이 이 코드의 뜻을 이렇게 정한다 — "세 계좌 전부에 중도 불이익이 걸려 **어느 배분안도 이를 피하지 못한다.** 화면은 배분 비교보다 이 사실을 앞세워야 한다."

그런데 GC-21에서 `isa_first` 안은 경고가 하나도 없다. **중도 불이익을 완전히 피하는 배분안이 실제로 존재한다.** 안내는 그 반대를 말하고 있고, 계약은 화면에 이 안내를 **배분 비교보다 앞세우라**고까지 지시한다. 결과적으로 사용자는 "어느 쪽을 골라도 불이익을 피할 수 없다"고 읽고, 실제로 피할 수 있는 선택지를 지나칠 수 있다.

세법 쪽 근거는 M2와 같다. `isa.early_termination.clawback`(조특법 §91조의18⑦)이 3년 경과 후에는 성립할 수 없으므로, ISA에는 걸릴 불이익이 없다.

### 원인 — 8.4는 고쳤고 8.5는 안 고쳤다

수정 커밋 `1a33f7e`의 `engine-interface.md` 변경분에 `all_accounts_have_early_exit_penalty`가 **한 번도 등장하지 않는다.** 8.4절의 경고 조건에는 `isa_lock_in_years_remaining > 0`이 추가됐지만, **같은 사실에 의존하는 8.5절의 안내 조건은 여전히 horizon 값만 본다.**

M2 수정이 "ISA에 중도 불이익이 걸린다"는 전제를 조건부로 바꿨는데, 그 전제를 공유하던 다른 출력이 함께 따라오지 못한 것이다. **1차의 M1이 D17의 파급을 배분 탐색이 못 따라간 것이었다면, M3은 M2의 파급을 비교 안내가 못 따라간 것이다.** 같은 형태의 실패가 두 번 났다.

### 판정 요청

**계약 8.5절의 조건을 8.4절과 맞추는 것이 맞다고 본다** — `all_accounts_have_early_exit_penalty`가 horizon만이 아니라 **실제로 세 계좌 전부에 경고가 걸렸는지**를 보게 하는 것이다. 그렇게 하면 코드의 이름과 뜻이 일치하고, 앞으로 경고 조건이 또 바뀌어도 안내가 자동으로 따라온다.

다만 계약은 게이트 2에서 고정됐고 8.5절 변경은 관리자 승인 사안이다. 룰셋은 고칠 것이 없다.

## 8.7 반복 불일치 · 에스컬레이션 (2차)

**에스컬레이션할 건 없다.**

- **M1 · M2** — 해소됐다. 반복 불일치가 아니다.
- **M3** — 이번이 **1회차**다. 헌장 6.4절의 "같은 케이스 3회 반복 불일치"에 해당하지 않는다.

GC-21은 1차(M2)와 2차(M3)에서 연속으로 불일치가 났으나 **원인이 다르다.** M2는 성립하지 않는 경고를 낸 것이고, M3은 그 경고를 없앤 뒤 남은 안내가 모순된 것이다. 같은 케이스에서 같은 원인이 반복된 것이 아니므로 3회 규칙의 대상이 아니다. **다만 GC-21은 이제 두 번 걸렸다** — 세 번째가 나오면 그때는 에스컬레이션한다.

## 8.8 다음 단계로 넘기는 사항 (2차)

- **관리자** — 판정 하나. M3을 계약 8.5절 수정으로 처리할지(8.6절). 게이트 2 고정분이므로 승인이 필요하다.
- **`calc-engine-dev`** — M3. 8.4절을 고칠 때 같은 사실에 의존하는 8.5절이 함께 움직여야 했다. 1차 리포트 6절에서 넘긴 `schema_version` 표기 정정(3.0절)은 3.1.0으로 갱신되며 해소됐다.
- **`qa`** — 게이트 4 검사에 하나 추가를 권한다. **`comparison_note_codes`가 주장하는 사실이 같은 응답의 `plans[].warnings`와 모순되지 않는지.** M3은 응답 안에서 자기모순이므로 골든 케이스 없이도 기계적으로 잡을 수 있다. 1차에서 넘긴 항목(계약 9절 드리프트 방지 규약, O1 화면 처리)은 그대로 남아 있다.
- **4단계 이후의 나 자신** — M3 수정 후 GC-21·GC-27·GC-18a를 다시 대조한다(안내를 끄는 수정이 또 과할 수 있다). 청년 IRP 우대 통과 시 재검토 항목은 1차 7절 그대로다.

---

# 9. 3차 재검증 — M3 수정 대조 (2026-08-09)

대상 커밋 `0ef9b52`, 계약 `schema_version` 3.1.0 → **3.2.0**(minor).

**독립성 유지.** `src/`를 한 줄도 읽지 않았다. 엔진 유닛이 세운 **불변식 테스트 파일도 열지 않았다** — 그것도 코드 쪽 논리다. 계약은 8.5절과 5.3절만 읽었다.

## 9.1 결과 요약 (3차)

| 구분 | 건수 |
|---|---|
| 대조한 케이스 | **35** (기존 34 + 신규 1) |
| 일치 | **35** |
| 불일치 | **0** |
| 해소 확인 | **M1 · M2 · M3 전부 해소 상태 유지** |
| GC-21 관련 신규 발견 | **없음** — 3회 규칙 미발동 |

## 9.2 M3 해소 — 그리고 조건이 실제로 결과 기반인지 확인했다

M3은 "고쳤다"만으로는 충분하지 않은 종류였다. **같은 증상을 없애는 손쉬운 방법이 따로 있었기 때문이다** — "의무가입기간이 경과했으면 안내를 끈다"는 **입력값 기반** 조건. GC-21만 보면 두 구현이 구별되지 않는다. 둘 다 안내를 끈다.

그래서 **GC-29를 새로 만들었다.** 의무가입기간은 경과했는데(잔여 0년) ISA 총 납입한도가 소진되어 자금이 연금계좌로만 가는 상황이다. 모든 배분안이 연금 중도인출 경고를 지므로 **"어느 배분안도 중도 불이익을 피하지 못한다"는 진술이 참이고 안내는 나가야 한다.** 입력값 기반 구현이라면 여기서 잘못 껐을 것이다.

| 케이스 | 잔여 의무가입기간 | 경고 없는 배분안 | 기대 | 결과 |
|---|---|---|---|---|
| GC-18a | 3년 | 없음 (3안 전부 경고) | 안내 **나감** | 나감 |
| GC-27 | 1년 | 없음 (3안 전부 경고) | 안내 **나감** | 나감 |
| **GC-21** | **0년** | **있음** (`isa_first`가 빈 배열) | 안내 **안 나감** | **안 나감** |
| **GC-29** | **0년** | 없음 (2안 전부 연금 경고) | 안내 **나감** | **나감** |

**GC-21과 GC-29는 의무가입기간 경과라는 같은 입력을 공유하면서 안내의 정답이 반대다.** 둘 다 맞았으므로 조건이 입력이 아니라 **결과의 사실**에 걸려 있음이 확인된다. 엔진 유닛의 구조적 수정 주장은 사실이다.

`isa_lock_in_already_elapsed`(info)는 GC-21·GC-29 둘 다에서 나갔다. 이 코드는 "입력과 현실이 어긋난다"는 사실 통지이므로 안내 여부와 독립적으로 나가는 것이 맞다.

**반대편도 죽지 않았다.** GC-18a(잔여 3년)·GC-27(잔여 1년)에서 ISA 추징 경고와 비교 안내가 모두 그대로다. M2 때 확인한 것이 M3 수정 후에도 유지된다.

## 9.3 M1 · M2 유지 확인 — 세 번째 수정이 앞의 둘을 되돌리지 않았다

| 확인 항목 | 2차 값 | 3차 값 | 판정 |
|---|---|---|---|
| GC-19 개정안분 | 1,386,000 (IRP 6,000,000) | **1,386,000 (IRP 6,000,000)** | 유지 |
| GC-19c | 1,386,000 (IRP 6,000,000) | **1,386,000 (IRP 6,000,000)** | 유지 |
| GC-23 | 1,485,000 (IRP 9,000,000 + ISA 3,000,000) | **동일** | 유지 |
| GC-19b · GC-23-oracle (인정 격리) | 1,386,000 · 1,485,000 | **동일** | 유지 |
| GC-19 확정분 · GC-20 개정안분 (치환 금지) | IRP 3,000,000 | **IRP 3,000,000** | 유지 |
| GC-21 ISA 추징 경고 | 없음 | **없음** | 유지 |
| GC-27 · GC-18a · GC-18d ISA 경고 | 있음 | **있음** | 유지 |

## 9.4 확정 시나리오 값 불변 — 세 번째도 움직이지 않았다

2차와 같은 방식으로 값 단위 대조했다. 대상은 확정 시나리오 25건이고, 필드는 `pension_combined_credit_limit_krw` · `pension_combined_credit_remaining_krw` · `pension_contribution_limit_remaining_krw` · 계좌별 `contribution_limit_remaining_krw` · `credit_eligible_limit_remaining_krw` · `tax_free_limit_krw` · `clamped_to_zero` · 최대공제안의 `allocations`(연·월·`limited_by`) · `deterministic_benefit` 4개 값 · `delta_vs_baseline_krw` · `unallocated_annual_krw` · `monthly_rounding_residual_krw`다.

**전부 1차·2차와 동일하다.** 경계 케이스의 특징적인 값들이 그대로 나온다 — GC-04·GC-09의 잔차 8, GC-22의 잔차 7과 대안안 14, GC-10의 ISA 잔여 70,000,000, GC-12의 60,000,000, GC-15/16/17의 추가한도 3,000,000 / 1,000,000 / 1,800,000.

**조건부 치환 3건(GC-24·25·26)도 2차와 같다.** GC-25(총급여 55,000,000)와 GC-26(55,000,001)의 개정안 세액공제액이 여전히 둘 다 1,485,000이면서 배분은 IRP 3,000,000 대 **9,000,000**으로 반대다. 조건이 1원 경계에서 정확히 뒤집힌다.

## 9.5 계약 5.3절 변경 확인 — 세법과 맞다

`contribution_limit_shared_with`와 `credit_limit_shared_with`의 성질이 다르다는 설명이 추가됐다. 실제 값으로 확인했다(GC-01).

| 계좌 | `contribution_limit_remaining_krw` | `credit_eligible_limit_remaining_krw` | `contribution_limit_shared_with` | `credit_limit_shared_with` |
|---|---|---|---|---|
| `retirement_pension` | 18,000,000 | **9,000,000** | `[annuity_savings]` | `[annuity_savings]` |
| `annuity_savings` | 18,000,000 | **6,000,000** | `[retirement_pension]` | `[retirement_pension]` |
| `isa` | 20,000,000 | `null` | `[]` | `[]` |

**설명이 세법과 맞다.** 납입 한도는 `pension.contribution.annual_limit`이 계좌 합산 1,800만원 하나로 정하므로 두 계좌의 값이 **같다**(18,000,000). 반면 세액공제 대상 한도는 `pension.credit.limit.combined`(합산 900만원)와 `pension.credit.limit.annuity_savings`(연금저축 단독 600만원)가 겹쳐 걸리므로 **값이 정당하게 다르다**(9,000,000 대 6,000,000).

그리고 **어느 쪽도 더하면 안 된다.** 더하면 납입 한도는 36,000,000(실제 18,000,000), 공제 한도는 15,000,000(실제 9,000,000)이 되어 둘 다 실제의 두 배 가까이 부풀려진다. 5.3절의 경고가 필요한 이유다.

## 9.6 GC-21 — 3회 규칙에 대한 판정

**이번에 GC-21에서 나온 새 발견은 없다.** 골든 케이스가 요구한 네 가지(ISA 추징 경고 소멸, `isa_lock_in_already_elapsed` 출현, 연금 경고 유지, `all_accounts_have_early_exit_penalty` 소멸)가 전부 충족됐고, 그 밖의 필드도 값이 어긋난 곳이 없다.

따라서 **에스컬레이션하지 않는다.** GC-21은 1차(M2)·2차(M3)에서 걸렸고 3차에서는 깨끗하다. 관리자가 지정한 "이번에 또 나오면 세 번째" 조건이 발동하지 않았다.

다만 기록으로 남긴다 — **GC-21은 이 제품에서 가장 자주 깨진 자리다.** 두 번 모두 원인이 달랐고(성립하지 않는 경고를 낸 것 / 그 경고를 없앤 뒤 남은 안내가 모순된 것), 두 번 모두 **"어떤 사실이 조건부로 바뀌었는데 그 사실에 의존하던 다른 출력이 따라오지 못한" 형태**였다. 이 케이스가 그런 형태를 유난히 잘 드러내는 이유는 **입력 자체가 모순이기 때문**이다(의무가입기간이 끝난 사람이 "그 기간 안에 쓰겠다"고 답한 상태). 앞으로 경고·안내 조건을 건드리는 수정이 있을 때마다 GC-21과 GC-29를 함께 돌리기를 권한다.

## 9.7 불변식 17개에 대한 관찰

이번 수정에서 불변식 17개 × 144건 요청 행렬이 세워졌다고 보고받았다. **테스트 파일을 열지 않았으므로 그 내용을 검증하지 않았고, 검증했다고 말하지 않는다.** 이 유닛이 확인한 것은 골든 케이스 35건의 입출력뿐이다.

다만 방향에 대해 한 가지는 적어 둔다. **불변식은 골든 케이스를 대체하지 않는다.** 불변식은 "응답이 자기모순이 아닌가"를 보고 골든 케이스는 "숫자가 세법과 맞는가"를 본다. M1은 자기모순이 아니었다 — 응답 어디에도 어긋난 곳이 없었고 단지 **세법상 최적이 아니었을 뿐**이다. 그것은 세법에서 독립적으로 답을 낸 골든 케이스만 잡을 수 있었다. 반대로 M3은 자기모순이었으므로 불변식이 잡았을 것이다. **둘 다 필요하고 서로를 대신하지 못한다.**

## 9.8 남은 사항

1·2차에서 넘긴 것 중 이번에 해소되지 않은 항목이다. 새로 추가한 것은 없다.

- **`qa`** — 계약 9절 드리프트 방지 규약(`computeFundUseHorizonBoundaries`와 `compute`의 `boundaries` 동일성)은 이번에도 대조하지 않았다. 1차 머리말 `open_questions` 그대로 남아 있다.
- **`designer`·`web-dev`** — O1(과세특례가 배제된 ISA에 비과세 한도가 표시됨, 1차 4절). 화면 처리 사안으로 남아 있다.
- **4단계 이후의 나 자신** — 청년 IRP 우대가 국회를 통과하면 D17 재검토와 GC-19·19b·19c·20·23·23-oracle·24·25·26의 기대값 재산출. 인출 단계 v2 착수 시 미사용 규칙 4건의 골든 케이스 추가.
