/**
 * 「연금고갈 시뮬레이션」 탭의 기준 상수 — `src/design/연금고갈_시뮬레이션.html`
 * (소유자 초안, 142줄)의 등식·구조를 그대로 옮기고, tax-domain의 검증
 * (`docs/stage-1-discovery/tax-rules-report.md` 33절, 26차)에 따라 상수 값·
 * 라벨·출처를 정정했다(D84 판정 4).
 *
 * **이 탭은 개인 세액 계산이 아니라 공적 기금의 시나리오 교육 도구다**
 * (D84 판정 2) — 그래서 이 파일의 단위는 이 저장소의 다른 곳(세액·배분,
 * 항상 원 단위)과 다르다. 기금·수입·지출은 국민연금 공식 통계가 쓰는
 * 그대로 **조원**(1조원 = 10,000,000,000,000원) 단위다.
 *
 * **출처 — 33.8절 24행 표를 그대로 옮겼다.** 이 파일 상수마다 그 표의
 * 행 번호(#)를 단다. **6개는 33.9절 권고대로 정정했고, 19·20은 그대로
 * "출처 없음"이 아니라 제5차 재정추계 기본가정으로 대체됐다**(33.8·33.9).
 * 여전히 출처가 부분적이거나 방향만 확인된 항목(#21·#22)은 그 사실을
 * 그대로 남긴다 — 확인되지 않은 것을 확인된 것처럼 적지 않는다.
 */

// ---------------------------------------------------------------------------
// 슬라이더 정의 — 핵심 가정(core) 셋 + 고급 설정(advanced) 다섯.
// ---------------------------------------------------------------------------

/**
 * @typedef {object} DepletionSliderParam
 * @property {string} id
 * @property {'core'|'advanced'} group
 * @property {string} label
 * @property {number} min
 * @property {number} max
 * @property {number} step
 * @property {number} default
 * @property {string} unit
 * @property {number} decimals 표시 소수 자리
 */

/** @type {DepletionSliderParam[]} */
export const DEPLETION_SLIDER_PARAMS = [
  // [33.8 #19, 33.9] 제5차 재정추계 기본가정 — 기금투자수익률 4.5%(민감도
  // 표에 명시). 초안 4.6%는 근거를 찾지 못해 4.5%로 내렸다(영향은 작다 —
  // 소진연도 1년 이내).
  { id: 'ror', group: 'core', label: '기금운용수익률', min: 2, max: 9, step: 0.1, default: 4.5, unit: '%', decimals: 1 },
  // [33.5 (1), 33.8 #11·#12] 최종 도달치 13%는 법정 스케줄의 끝값(아래
  // `CONTRIBUTION_RATE_*`와 같은 근거). 슬라이더 상한 22%는 제5차 재정추계
  // 「재정목표 달성을 위한 필요보험료율」(17.86~23.73%)이 근거다 — 13%를
  // 넘는 구간은 법이 아니라 가정이라는 사실을 화면이 함께 말해야 한다
  // (`ui/depletion-panel.js`).
  { id: 'rate', group: 'core', label: '보험료율 (최종)', min: 9, max: 22, step: 0.5, default: 13, unit: '%', decimals: 1 },
  // [33.8 #3] 국민연금연구원 중기재정전망 — 2026년 말 2,140만 → 2030년 말
  // 2,051만(연 −1.05%, 초안 −1.0%와 사실상 일치).
  { id: 'sub', group: 'core', label: '가입자 연평균 증감률', min: -3, max: 0.5, step: 0.1, default: -1.0, unit: '%', decimals: 1 },
  // [33.8 #20, 33.9] 제5차 재정추계 기본가정 — 임금상승률 3.7%(민감도 표
  // 「기본가정(3.7%)」). 초안 3.8%는 근거 없어 3.7%로 내렸다.
  { id: 'wage', group: 'advanced', label: '임금상승률', min: 1, max: 6, step: 0.1, default: 3.7, unit: '%', decimals: 1 },
  // [33.8 #15] 제5차 재정추계 기본가정에서 파생 — 명목임금 3.7% − 실질임금
  // 1.7% = 2.0%. 정정 없음(33.9 "물가상승률 기본값 2.0% 유지").
  { id: 'cpi', group: 'advanced', label: '물가상승률', min: 0.5, max: 4, step: 0.1, default: 2.0, unit: '%', decimals: 1 },
  // [33.6 (4), 33.9] **기본값을 3→0으로 내렸다.** 수급자 곡선
  // (`BASE_RECIPIENTS_BREAKPOINTS_10K`)이 이미 통계청 장래인구추계(기대수명
  // 개선 포함)를 깔고 있어, 기본값 3이 살아 있으면 그 효과가 두 번
  // 계상된다(다른 편차 손잡이 `age`·`ben`은 기본값에서 이미 편차 0이다).
  // 이 손잡이는 **공식 추계 대비 추가** 증가를 묻는 시나리오용으로 남긴다.
  { id: 'life', group: 'advanced', label: '기대수명 증가 (2070년까지)', min: 0, max: 8, step: 0.5, default: 0, unit: '년', decimals: 1 },
  // [33.5 (3), 33.8 #13] 65세는 「현행」이 아니라 **최종**(법정 상한)이다 —
  // 「국민연금법」 제61조·법률 제8541호 부칙 제8조(1969년생 이후 65세).
  // 2026년 현재 실제 적용 연령은 63세(1961~64년생)다. 슬라이더 기본값 65는
  // "공식 수급자 곡선으로부터의 편차 0"을 뜻해 계산상 문제는 없다 — 라벨만
  // 정정한다(아래 `depletion-copy.js`).
  { id: 'age', group: 'advanced', label: '최종 수급개시연령(현행 법정 상한 65세, 1969년생부터 적용)', min: 65, max: 70, step: 1, default: 65, unit: '세', decimals: 0 },
  // 수급자수 보정 범위 자체(±20%)의 산정 근거는 33절이 확인하지 못했다 —
  // TODO(tax-domain): 출처, 확인 전.
  { id: 'ben', group: 'advanced', label: '수급자수 보정', min: -20, max: 20, step: 1, default: 0, unit: '%', decimals: 0 },
];

// ---------------------------------------------------------------------------
// 초기 상태 (2026년 시점) — 조원 단위.
// ---------------------------------------------------------------------------

// [33.1, 33.8 #1] 국민연금연구원 「국민연금 중기재정전망(2026~2030)」(2026)
// — 적립기금 2025년 말 1,458조원(정확히 일치). **「전망 재현」을 택한다**
// (33.7, 33.10 6번) — 실제 실적(2026.4월 말 1,670.7조, #18)을 출발점에
// 넣지 않는다. 둘 중 하나만 고를 수 있고, 실적을 넣으면 2030 대조 장치
// (`OFFICIAL_2030_CHECK`)가 깨진다(공식 대비 +9.9%). 대신 실적은 화면에
// 별도 고지 한 줄로만 보인다(`ACTUAL_FUND_BALANCE`, 아래).
export const INITIAL_FUND_TRILLION_KRW = 1458;
// [33.1, 33.8 #2] 동일 — 가입자 2026년 말 2,140만 명(정확히 일치).
export const INITIAL_SUBSCRIBERS_10K = 2140;
// [33.2, 33.8 #9] **`평균` 소득월액이 아니다 — 역산 상수다.** 국민연금연구원
// 중기재정전망의 2026년 보험료수입 68.6조를 `subs×12×9.5%`로 되맞추도록
// 역산했다(`68.6 ÷ (2140×12×0.095) = 2.812`). **실측 평균(2026년 A값
// 319만 3,511원)과 다르다** — 납부예외자·장기체납자가 가입자 수(2,140만)에
// 포함되지만 보험료를 내지 않기 때문이다. **실측값으로 바꾸면 보험료수입이
// +13.6% 과대해진다(33.2) — 절대 실측 평균으로 대체하지 않는다.** 화면에는
// 이 이름(「평균」)으로 노출하지 않는다 — `ui/depletion-panel.js`도 이
// 값을 라벨로 보이지 않는다.
export const EFFECTIVE_WAGE_BASE_PER_SUBSCRIBER = 2.812;
// [33.2, 33.8 #10] **`평균` 급여월액이 아니다 — 역산 상수다.** 같은
// 보고서의 2026년 급여지출 56조를 `recip×12`로 되맞추도록 역산했다
// (`56 ÷ (828×12) = 0.5636`). **실측(2025년 노령연금 월평균 67만 9,924원)
// 보다 낮다** — 분모가 일시금 포함 828만이고 유족·장애연금이 평균을
// 끌어내리기 때문이다. **실측 평균으로 바꾸면 급여지출이 +20.6% 과대해진다
// — 절대 대체하지 않는다.**
export const EFFECTIVE_BENEFIT_BASE_PER_RECIPIENT = 0.5636;
// [33.7, 33.10 6번] **화면 고지용 — 시뮬레이션 계산에는 쓰지 않는다.**
// 국민연금기금운용본부 「기금 적립현황 공시」(2026) — 2026년 4월 말 실제
// 적립금 1,670.7조원(수익률 호조로 전망을 크게 앞섰다). 이 시뮬레이터는
// "전망 재현" 모델이라 이 실적을 출발값에 넣지 않는다 — 그 사실을 화면이
// 밝혀야 한다(`ui/depletion-panel.js`의 안내 한 줄).
export const ACTUAL_FUND_BALANCE = {
  asOf: '2026년 4월 말',
  trillionKrw: 1670.7,
  source: '국민연금기금운용본부 · 기금 적립현황 공시 · 2026',
};

// [33.5 (1), 33.8 #11 — `CONTRIBUTION_RATE_*`와 같은 근거] 법정 보험료율
// 인상 스케줄의 종료치. `ui/depletion-panel.js`가 슬라이더 값이 이 값을
// 넘으면 "법정 스케줄이 아닌 가정" 경고를 낸다 — 숫자를 UI 파일에 직접
// 적지 않고 여기서만 정의한다.
export const STATUTORY_RATE_CEILING_PERCENT = 13;
// [33.5 (3), 33.8 #13 — 위 age 슬라이더 주석과 같은 근거] 법정 최종
// 수급개시연령(1969년생부터). 이 값을 넘는 슬라이더 값은 법·개정안이
// 아닌 순수 가정이다.
export const STATUTORY_AGE_CEILING_YEARS = 65;

/** 시뮬레이션 시작·끝 연도, 소진 뒤 몇 년 더 그리고 멈출지. */
export const SIMULATION_START_YEAR = 2026;
export const SIMULATION_END_YEAR = 2115;
/** 소진 연도(그 해 말 잔액이 처음 음수가 되는 해, 공식 관례 —
 * `tax-rules-report.md` 33.4) 뒤 이만큼 더 그리고 멈춘다 — 궤적이 "소진
 * 이후"를 짧게라도 보여주게 하려는 것뿐, 그 뒤 값 자체는 의미가 없다
 * (기금은 이미 0 아래다). */
export const YEARS_TO_DRAW_AFTER_DEPLETION = 2;

// [33.5 (1), 33.8 #11] 「국민연금법」 제88조 및 부칙 제4조(일부개정 법률
// 제20903호, 2025.4.2 공포, 2026.1.1 시행) — 현행 9%를 2026년부터 매년
// 0.5%p씩 8년간 인상해 2033년 13%에 이르게 한다. 대조: 2026년 9.5%/2027
// 10.0%/…/2033 13.0% — 보건복지부 보도자료(2025.3.20)의 연도별 표와
// 여덟 해 모두 일치(조문·통과 판정).
export const CONTRIBUTION_RATE_BASE_PERCENT = 9;
export const CONTRIBUTION_RATE_ANNUAL_STEP_PERCENT = 0.5;
/** 인상이 시작되는 기준 연도 — `9 + 0.5×(y−CONTRIBUTION_RATE_BASE_YEAR)`. */
export const CONTRIBUTION_RATE_BASE_YEAR = 2025;

// ---------------------------------------------------------------------------
// 수급자 수 기준 곡선 — (연도, 만 명) 꺾은선 보간점.
// ---------------------------------------------------------------------------

// [33.1, 33.8 #4·#5, 33.8 21] 2026년 828만·2030년 1,006만(둘 다 「일시금
// 포함 전체 수급자」 계열 — 연금수급자만 세는 계열과는 다르다, 33.1)은
// 국민연금연구원 중기재정전망과 정확히 일치한다. **2035년 이후 앵커
// (2035~2100)는 그 보고서가 2030년까지만 다뤄 대응 표를 찾지 못했다**
// (33.8 #21, 출처 없음으로 남는다) — 다만 제도부양비 대조(33.6(2))가
// 2060년까지 ±13% 안에서 이 곡선을 간접 지지한다. 화면에는 "2031년
// 이후는 공식 전망이 공개되지 않아, 제5차 재정추계의 부과방식비용률에
// 맞도록 보정한 곡선"이라는 사실을 밝힌다(`ui/depletion-panel.js`).
export const BASE_RECIPIENTS_BREAKPOINTS_10K = [
  [2026, 828], [2030, 1006], [2035, 1230], [2040, 1450], [2045, 1620],
  [2050, 1750], [2055, 1810], [2060, 1790], [2065, 1720], [2070, 1630],
  [2080, 1420], [2090, 1220], [2100, 1050],
];

// ---------------------------------------------------------------------------
// 가입자 증감률에 얹는 "충격 배수" 구간 — 베이비부머 은퇴 집중 구간 등을
// 거친 근사.
// ---------------------------------------------------------------------------
// [33.8 #22, 33.9] 통계청 「장래인구추계: 2022~2072년」의 생산연령인구
// (15~64세)로 방향은 확인된다 — 2020년대 연평균 약 −0.94%/년, 2030년대
// 약 −1.6%/년, 2040년대 이후 약 −1.8%/년(가입자 ≠ 생산연령인구이므로
// 1:1 대응은 아니다, 방향 확인까지). **관리자 판정 — 2050~2069 구간을
// 1.5→1.8로 올린다**(통계청 2040년대 이후 경로에 맞춘다). 2070년 이후
// 구간(1.0)은 이번 판정에서 손대지 않는다.
export const SUBSCRIBER_SHOCK_WINDOWS = [
  { beforeYear: 2035, multiplier: 1.0 },
  { beforeYear: 2050, multiplier: 1.8 },
  { beforeYear: 2070, multiplier: 1.8 },
  { beforeYear: Infinity, multiplier: 1.0 },
];

// ---------------------------------------------------------------------------
// 수급개시연령·기대수명 슬라이더가 실제 수급자 수·급여에 스며드는 구간
// (선형 전이 — phase-in). 33.5 (3) — 2011년 개정이 60→65세를 20년
// (2013~2033)에 걸쳐 올린 전례와 견주면 12년(2030~2042)은 빠른 편이다 —
// 법도 개정안도 아닌 순수 가정으로 남는다. TODO(tax-domain): 더 나은
// 근거, 확인 전.
// ---------------------------------------------------------------------------
export const AGE_PHASE_IN_START_YEAR = 2030;
export const AGE_PHASE_IN_END_YEAR = 2042;
export const LIFE_PHASE_IN_START_YEAR = 2026;
export const LIFE_PHASE_IN_END_YEAR = 2070;

// [33.6 (3), 33.8 #24] 수급개시연령 1년 상향 → 그 해 수급자 수 −5%.
// 신규 노령연금 진입(연 50~60만 명) ÷ 전체 수급자(1,000만 명) ≈ 5~6% —
// 방향·크기 모두 통과.
export const RECIPIENTS_PER_AGE_YEAR_DELTA = 0.05;
/** 기대수명 1년 증가 → 그 해 수급자 수 증가율(전이 완료 기준). */
export const RECIPIENTS_PER_LIFE_YEAR_DELTA = 0.05;
// [33.6 (3), 33.8 #24, 관리자 판정] **0으로 뗀다.** 초안의 +1.2%/세는
// 현행법(의무가입 상한 60세 미만)만으로는 성립하지 않는다 — "가입상한연령도
// 함께 오른다"는 입법되지 않은 전제가 있어야 서는 계수다(33.6 (3)). 그
// 전제 없이 이 계수를 켜 두면 화면이 스스로 세우지 않은 가정을 숫자로
// 실어 나른다. **가입상한 상향이 실제로 입법되면 되살릴 값은 0.012**
// (신규 코호트 1/전체가입자 40 ≈ 2.5%의 절반 수준, 근거는 보수적이라
// 33.6 (3)이 "크기는 절반"이라 적었다) — 그 전까지는 0.
export const INCOME_PER_AGE_YEAR_DELTA = 0;
/** [비활성 상태의 참고값] 가입상한연령 동반 상향이 입법되면 되살릴 계수. */
export const INCOME_PER_AGE_YEAR_DELTA_IF_ENROLLMENT_CEILING_RAISED = 0.012;
// [33.6 (3), 33.8 #14] 「국민연금법」 제51조제1항 단서 — 가입기간 20년
// 초과 1년마다 본문 금액에 1천분의 50(=5%)을 더한다. 1년 더 내고 1년
// 늦게 받으면 정확히 +5%다 — 계수 넷 중 가장 튼튼한 근거.
export const OUTGO_PER_AGE_YEAR_DELTA = 0.05;

// ---------------------------------------------------------------------------
// 제도 성숙 효과 — 가입 기간이 길어질수록 1인당 급여가 임금상승률보다
// 빠르게 오르는 몫.
// ---------------------------------------------------------------------------
// [33.6 (2), 33.9 "고칠 것이 없다"] 노령연금 평균 가입기간이 현재 약
// 19년에서 성숙 후 27년 안팎으로 가는 데 30년이 걸린다고 보면
// `(27/19)^(1/30) − 1 ≈ 1.19%/년` — 1.3%p는 이 자릿수 안이다. 부과방식
// 비용률 역산 대조(제5차 재정추계 표)로도 2040~2065 구간 ±13% 안에서
// 지지된다. **한계 — 2070년 이후**: `matur`가 2068년에 0이 되며 후반
// 비용률을 제5차 대비 −21~−23% 과소평가한다(제5차는 2080년에 정점을
// 찍는데 이 모델은 2065년경 정점 후 하락한다) — 소진연도가 2070년대
// 이후로 나오는 조합에서는 이 과소평가가 결과를 낙관 쪽으로 민다(화면
// 한계 문구 필요, `ui/depletion-panel.js`).
export const MATURATION_EFFECT_START_YEAR = 2055;
export const MATURATION_EFFECT_END_YEAR = 2068;
export const MATURATION_EFFECT_ANNUAL_RATE = 0.013;

// [33.6 (2), 33.8 #23] 0.45는 임의의 수가 아니다 — 반례 시험(0.45→1.0)이
// 부과방식비용률 대조를 2060년 +47%·2093년 +81%로 파탄시킨다. 기존
// 연금은 물가에만 연동되고 신규 연금만 임금(A값)에 연동되므로, 스톡
// 평균의 임금 전가가 1보다 작은 것은 구조적으로도 옳다.
/** 급여(1인당) 상승률 = 물가상승률 + 임금-물가 차이의 이 몫 + 성숙효과. */
export const BENEFIT_WAGE_BLEND_SHARE = 0.45;

// ---------------------------------------------------------------------------
// 2030년 검증 대조값 — 이 모델의 2030년 계산값과 나란히 보인다(D84 판정 4
// "모델 등식의 방향·크기 검증(2030 대조값 포함)").
// ---------------------------------------------------------------------------
// [33.1, 33.8 #6·#7·#8, 관리자 판정] **출처 라벨 정정 — 「공식」이 아니라
// 국민연금연구원 「국민연금 중기재정전망(2026~2030)」(2026)이다.** 제5차
// 국민연금 재정추계(2023)와는 다른 문서다 — 제5차는 2025년 개혁(9%→13%
// 단계 인상) 반영 전 현행제도 기준이라 2030년 적립금이 1,898조에 이르는
// 경로 자체가 없다(제5차의 최대 적립금은 2040년 1,755조). **두 문서를
// 한 화면에서 섞지 않는다** — 대조 앵커(이 값)는 중기전망, 기본가정
// (수익률·임금 등)은 제5차, 서로 다른 출처를 각자 단다(`OFFICIAL_2030_CHECK.source`).
// 값 자체는 셋 다 정확히 일치(33.1) — 보험료수입 91.6조·급여지출 81.7조·
// 적립금 1,898조(**2030년 말**).
export const OFFICIAL_2030_CHECK = {
  year: 2030,
  incomeTrillionKrw: 91.6,
  outgoTrillionKrw: 81.7,
  fundTrillionKrw: 1898,
  source: '국민연금연구원 · 국민연금 중기재정전망(2026~2030) · 2026',
};
